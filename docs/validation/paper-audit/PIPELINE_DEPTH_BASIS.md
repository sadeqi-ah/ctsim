# Pipeline Depth Basis and Table I Sleep Column Audit

## 1. Overview
This document audits the mathematical basis of effective pipeline depth in
Table V (`tab:depth`, `paper/paper.tex:1906-1927`), the origin of the `Sleep`
column in Table I (`tab:baseline`, `paper/paper.tex:1536-1542`), and the
currency of the derived scalability summary table.

---

## 2. Table V Pipeline Depth: Candidate Bases

The paper defines effective pipeline depth by Little's law:
$\Cdepth = \text{Throughput} \times \text{Latency}$.
The manuscript text (`:1902-1905`) and caption (`:1907-1909`) claimed that
Table V was obtained by applying Little's law directly to the columns of
Table I (`tab:baseline`).

Using raw data from `plots/topology/results/sweep_summary.csv` at the reference
operating point (`random`, $\Nnodes = 27$, $\text{loss} = 0.05$, 15 seeds), with
$\text{throughput} = \text{committed} / \text{end\_slot}$ per
`plots/topology/plot_topology.py:94`:

### Candidate Basis Formulations
1. **Product of Means:**
   $\text{mean}(\text{throughput}_i) \times \text{mean}(\text{avg\_latency}_i)$
2. **Mean of Per-Seed Products:**
   $\text{mean}(\text{throughput}_i \times \text{avg\_latency}_i)$
3. **Median of Per-Seed Products:**
   $\text{median}(\text{throughput}_i \times \text{avg\_latency}_i)$

### Full-Precision Candidate Values

| Protocol | Published | Prod of Means | Mean of Prods | Median of Prods |
|---|---:|---:|---:|---:|
| TOM (CE) | 1.00 | 1.00209373 | 1.00000000 | 1.00000000 |
| Paxos (CE) | 1.00 | 1.00056944 | 1.00000000 | 1.00000000 |
| 2PC (CE) | 1.00 | 1.00168935 | 1.00000000 | 1.00000000 |
| TOM (CI) | 1.00 | 1.00208497 | 1.00000000 | 1.00000000 |
| Paxos (CI) | 11.66 | 11.68504943 | 11.66152391 | 11.66131907 |
| 2PC (CI) | 21.25 | 21.31454078 | 21.25465016 | 21.43387097 |

### 2-Decimal Rounded Values and Candidate Verdicts

| Protocol | Published | Prod of Means | Mean of Prods | Median of Prods |
|---|---:|---:|---:|---:|
| TOM (CE) | 1.00 | 1.00 [AGREES] | 1.00 [AGREES] | 1.00 [AGREES] |
| Paxos (CE) | 1.00 | 1.00 [AGREES] | 1.00 [AGREES] | 1.00 [AGREES] |
| 2PC (CE) | 1.00 | 1.00 [AGREES] | 1.00 [AGREES] | 1.00 [AGREES] |
| TOM (CI) | 1.00 | 1.00 [AGREES] | 1.00 [AGREES] | 1.00 [AGREES] |
| Paxos (CI) | 11.66 | 11.69 [DIFFERS] | 11.66 [AGREES] | 11.66 [AGREES] |
| 2PC (CI) | 21.25 | 21.31 [DIFFERS] | 21.25 [AGREES] | 21.43 [DIFFERS] |

### Finding
The **mean of per-seed products** (`mean(throughput_i * avg_latency_i)`) is the
ONLY candidate basis that reproduces all six published values:
- For the four non-pipelined combinations (TOM CE, Paxos CE, 2PC CE, TOM CI),
  $\text{end\_slot} = \text{committed} \times \text{avg\_latency}$ holds on
  every seed identically, so throughput $\times$ latency $= 1.00000000$ exactly
  on every run.
- For Paxos (CI), the mean of per-seed products is `11.66152391` $\to$ `11.66`.
  (The product of aggregate means gives `11.68504943` $\to$ `11.69`).
- For 2PC (CI), the mean of per-seed products is `21.25465016` $\to$ `21.25`.
  (The product of aggregate means gives `21.31454078` $\to$ `21.31`; the median
  gives `21.43387097` $\to$ `21.43`).

### Repository Evidence for Origin of `11.66` and `21.25`
Searching for `11.66` and `21.25` across committed files confirms they were
generated by code using the mean of per-seed products:
- `docs/validation/patch_spec.md:65, 298, 981`:
  `depth: runs.csv / sweep_summary.csv, (committed/end_slot) x avg_latency,`
  `15 seeds -> 11.6615 and 21.2547`
- `docs/validation/addition8.md:150, 225, 226`:
  `depth: 11.661524 +- 0.015311 and 21.254664 +- 0.218137`
- `docs/validation/addition7/scripts/make_patch_spec.py:81, 120, 177`:
  `runs.csv, committed/end_slot x avg_latency, 15 seeds -> 11.6615 and 21.2547`

---

## 3. Table I Sleep Column Audit and Provenance Settlement

### Comparison Across Sweep Summary Sources
The reference configuration (`random`, $\Nnodes = 27$, $\text{loss} = 0.05$) is
present in both sweep files:
1. `plots/scalability/results/sweep_summary.csv:56-61` (primary source)
2. `plots/topology/results/sweep_summary.csv` (cross-check)

Both files record a `sleep` column (field 17). Computing the 15-seed arithmetic
mean of `sleep` on both files yields:

| Protocol | Scalability Sweep Mean | Topology Sweep Mean | Identical? |
|---|---:|---:|:---:|
| TOM (CI) | 3899.86666667 | 3899.86666667 | True |
| Paxos (CI) | 4434.26666667 | 4434.26666667 | True |
| 2PC (CI) | 5033.73333333 | 5033.73333333 | True |
| TOM (CE) | 0.00000000 | 0.00000000 | True |
| Paxos (CE) | 0.00000000 | 0.00000000 | True |
| 2PC (CE) | 0.00000000 | 0.00000000 | True |

Both sweep CSVs yield strictly identical means across all six protocols.

### Table Generator Origin and Canonical Citation
Table I is built by `plots/scalability/plot_summary_commit.py:85`, which reads
`plots/scalability/results/sweep_summary.csv` and outputs
`plots/scalability/table_summary_N27_loss05.csv` with `sleep=("sleep", "mean")`
rounded to 0 decimal places (`:103`):
```text
TOM (CI),...,3900.0
Paxos (CI),...,4434.0
2PC (CI),...,5034.0
TOM (CE),...,0.0
Paxos (CE),...,0.0
2PC (CE),...,0.0
```
Therefore, the canonical citation is
`plots/scalability/results/sweep_summary.csv:56-61` (matching the adjacent audit
row for the baseline table rows), with the topology sweep CSV recorded as an
identical cross-check.

### Verdict
**REPRODUCIBLE from committed data.** The Table I `Sleep` column values are the
exact integer roundings of the 15-seed means of the `sleep` column from
`plots/scalability/results/sweep_summary.csv:56-61`.

---

## 4. Manuscript Claim Correction

For the manuscript's claim about Table V's derivation to be true:
- The text and caption must NOT claim depth is the product of the displayed
  Table I aggregate columns (which would yield 11.69 and 21.31).
- The text and caption must state that throughput and latency are the means
  over the fifteen seeds, while depth is the mean over the fifteen seeds of the
  per-seed product of throughput and latency, rather than the product of the two
  printed columns.

---

## 5. Staleness Check for Derived Scalability Table (Report Only)

`plots/scalability/table_summary_N27_loss05.csv` is a committed derived table,
the same class of artifact audited for the topology table in issue 350.

Git commit history of the artifact, its raw input CSV, and simulator logic:
- Derived table:
  `git log -1 plots/scalability/table_summary_N27_loss05.csv` $\to$ `198ac57`
- Input sweep CSV:
  `git log -1 plots/scalability/results/sweep_summary.csv` $\to$ `cf05dd8`
  (data(sweeps): regenerate sweep_summary.csv after issue 257 quorum fix)
- Simulator logic:
  `git log -1 src/` $\to$ `36c506f` / `d6937dc` (issue 257 fix)

Commit `198ac57` is an ancestor of `cf05dd8`. The committed derived table
predates the post-issue-257 sweep regeneration of its input CSV.

**Status:**
**POSSIBLY STALE -- requires a run to confirm.**
Per project constraints, no sweep is executed and `regeneration` CI job remains
disabled.
