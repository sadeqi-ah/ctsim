# Non-blocking Two-Phase Commit over the CI pipeline.
seed = 42
phy_mode = "ci"
protocol = "2pc_pipeline"
num_proposals = 100
snapshot_interval = 1
max_slots = 100000

[network]
num_nodes = 27
topology = "random"
loss_rate = 0.05

[ci]
flood_repeats = 1
